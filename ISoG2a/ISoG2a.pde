/* boxes of shifting greys */

void setup() {
  size(600, 600);
  stroke(5); // thickness of border
  fill(127);
}

void draw() {
  /* draw boxes, overlap them, & cycle through greys inside each */
  rect(100, 100, 200, 200); // BOXES!
}
